interface:
  display_name: "Edge Remote Debug"
  short_description: "Remote debug Edge via CDP from WSL2"
  default_prompt: "Control the user's Edge browser via CDP bridge. Extract content from any open page, interact with web apps, scrape AI chat conversations. The user's Edge is already running with --remote-debugging-port=9222. Start the CDP bridge if needed before extraction."
